<h1>Edit Post</h1>

<%= form_for @post do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
  <%= f.text_field :title %>
  <%= f.text_area :content %>
</div>
<%= f.submit "Update Post", class: "btn btn-large btn-primary" %>
<% end %>